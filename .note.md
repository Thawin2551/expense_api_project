# Pydantic
We will use pydantic to build a class to keep a password and others data in secret and prevent injection

# Logic and Aggration
After we change format of send user_id directly we use to send token insted
we will start to do some logic and filtering data type

# Debug jwtToken
# Debug and uncomment include {"user": True} so the error was fix
because when i try to req post api to transaction it will relational to table user
so uncomment will told the request to grab user table and access user id

# Debug get_transaction_by_month and check wrong spelling
found prisma -> primsa
for item in summary_date 
and return wrong variable. so the problem is i must return transactions in get_by_month, but i acciendtally return transaction that without "s" in the end]

# Fix error at get_transaction_by_month again
the error was fix by adding {"category": True} to include data from Category Table to retrieve in transaction table

# Error was occured at get_transaction_summary
it return null when i try to sent a GET at /summary endpoint
so i was fix the error by using gemini to debug and i found that indentation in if statement was a problem

# Fix Deploy on Render (Prisma Problem and Render Folder)
fixed this error by add:
PRISMA_BINARY_CACHE_DIR = /opt/render/project/src/.binaries
we set this variable render will not remove cache after builded engine